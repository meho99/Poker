import types from './types'


const initialState = {
    cards: [

    ]
}

const Karty = (state = initialState, action) => {
    switch (action.type) {





        default:
            return state
    }
}

export default Karty;